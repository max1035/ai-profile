# H5 Demo

This repository is now a static H5 demo featuring three pages (Home, Live, Sports) that can be switched using the bottom navigation.

- Entry file: `index.html`
- Screenshot assets: `assets/` directory (place `home.png`, `live.png`, `sports.png` inside).

Open `index.html` directly in your browser to view the demo.
